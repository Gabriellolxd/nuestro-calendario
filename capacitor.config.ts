import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dani.nuestrocalendario',
  appName: 'nuestro-calendario',
  webDir: 'out'
};

export default config;
